#include <stdio.h>
#include <string.h>

int main(void){
    char str1 [1001], str2 [1001];
    scanf("%s", str1);
    scanf("%s", str2);
    if(strlen(str1) >= strlen(str2))
        printf("go");
    else
        printf("no");
}
